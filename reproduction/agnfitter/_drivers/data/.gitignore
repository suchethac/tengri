# The AGNFITTER-RX reference templates the notebook overlays live in the
# committed h5 grids under the repo-root data/ -- the driver reads only those,
# never an external checkout (see reproduction/agnfitter/README.md). Anything
# generated into this directory (repackaged DSPS grids, scratch downloads) is
# build output, not tracked.
*.h5
*.pickle
*.fits
