# Documentation build dependencies.
#
# One list, not two. This file used to restate the [docs] extra in
# pyproject.toml, and the two had drifted by exactly one entry --
# sphinx-gallery, which docs/conf.py names in `extensions`, so the build cannot
# start without it. CI installs this file and was unaffected; anyone following
# `pip install -e ".[docs]"` got a docs build that failed on import. Installing
# the extra from here keeps that from recurring, because there is nothing left
# to keep in step.
-e .[docs]
