# Tengri Refactor Plan: Lessons from Organic Growth

> **Status**: Phases 1, 4–7 complete. Phase 2 (god-file splits) and
> Phase 3 (settings vs. parameters) remain **partial**. **Phase II
> (SEDComponent migration) implemented 2026-05-04**: II-2.1 through
> II-5 are landed (stellar package consolidation, full
> StellarSEDComponent.apply with the 11-key contract,
> PipelineState as JAX pytree, DustSEDComponent, NebularSEDComponent
> with Cue/CloudyGrid/MAPPINGS dispatch, AGNSEDComponent, public-API
> `tengri.forward.build_components`, regression test at
> `tests/integration/test_orchestrator_jit.py`).
>
> **Phase II-3 partial closure 2026-05-05** ("pure Wilkinson"):
> per-component SED publication unified across the orchestrator —
> `RadioSEDComponent`/`XRaySEDComponent` now publish `sed_radio`/`sed_xray`
> (was `L_radio`/`L_xray`); standalone `DustEmissionSEDComponent`
> publishes `sed_dust_ir` (was `L_dust_emitted`); `NebularSEDComponent`
> always publishes both `sed_nebular` and `sed_shock` (zeros for the
> non-active branch); `DustSEDComponent.apply()` is now **additive**,
> preserving any non-stellar contribution accumulated upstream by
> computing `non_stellar_pre_dust = state.sed_intrinsic - sum(lnu_age)`
> before `sed_total = non_stellar + sed_attenuated_stellar + sed_ir`
> — this matches legacy semantics where stellar dust does not attenuate
> AGN/nebular/radio/xray, and makes chain order moot for those layers.
> `Posterior.sed_components()` migrated from
> `model._compute_sed_components` to `model.predict_via_orchestrator`,
> reading per-component SED arrays from `state.derived` and reconstructing
> stellar `sed_intrinsic` from the published `lnu_age` cube.
>
> **Phase II-3 is now closed (2026-05-06)** — the per-component
> publication unification was only the first half. The closure
> details for the second half (consumer migration of all five
> `predict_*` methods + the `Prediction` wrapper) are below.
>
> **Phase II-3 closure 2026-05-06** ("pure Wilkinson, complete"):
> all five `predict_*` consumers and the lazy `Prediction` wrapper
> are migrated. `_compute_sed_components` has **zero production
> callers** as of commit `6d5ee9f`. The legacy method is preserved
> only as an internal comparison helper for 5 parity-check tests
> (`tests/unit/test_fused_rest_sed.py`,
> `tests/integration/test_panchromatic_integration.py`). Sub-PR
> commit map:
>
> - **PR 5a** (`77704a8`): `predict_rest_sed` default-`wave` path
>   → `predict_via_orchestrator`. Bundled with two pre-existing
>   orchestrator gaps the legacy was masking — `met_alpha_fe`
>   plumbing in `StellarSEDComponent` (via `effective_metallicity`
>   shift on 3D SSP grids; 4D α-grids raise `NotImplementedError`)
>   and the `dust_emission=None` opt-out (factory was silently
>   coercing missing `dust_emission` to `"modified_blackbody"` and
>   re-emitting absorbed energy as a thermal blackbody, polluting
>   the long-wavelength tail).
> - **PR 5b'** (`b7dff1b`): `predict_line_fluxes` →
>   `state.derived["line_lums"]`. Replaces the original (incorrect)
>   PR 5b target — `predict_spectral_indices` doesn't actually
>   call `_compute_sed_components`. Fixes Cue line-luminosity
>   parity by (a) publishing `age_weights` from
>   `StellarSEDComponent` so `NebularSEDComponent` runs Cue in its
>   high-level (SSP-derived ionising spectrum) mode; (b) translating
>   `neb_logZ_gas` from user-facing Z/Zsun to absolute log10(Z)
>   via the LOG10_ZSUN offset (was the dominant 240× drift).
> - **PR 5c** (`63f036f`): `predict_sed_quantities` dispatched to
>   `predict_sed_quantities_via_orchestrator`. The "physics
>   blocker" on `luminosity_weighted_age_gyr` turned out to be a
>   `csp_integration='trapz'` vs `'dsps_native'` mismatch: with
>   dsps_native (orchestrator's canonical mode) all 15 fields agree
>   at sub-0.1%; only the trapz default shows the 12% drift. The
>   orchestrator value is energy-conserving by construction.
> - **PR 5d** (`0c72ac5` + `6d5ee9f`): `Prediction._ensure_lines`
>   then `Prediction._ensure_sfh` + `_ensure_sed` migrated. Both
>   helpers now reuse a single cached `_state` so SFH-only
>   consumers (mass, sfr_*) and SED-consuming properties (dn4000,
>   lum-weighted-age) share the same DSPS-canonical numerics.
>   Required interpolating `sfr_history` from the orchestrator's
>   grid (`spec.n_grid`, default 64) onto `model.age_yr`
>   (256 for parametric configs) so age-mask consumers still align.
> - **PR 5e** (`5f1d862`): `predict_rest_sed`'s custom-`wave`
>   branch now interpolates `state.sed_intrinsic` via `jnp.interp`
>   instead of re-routing through `_compute_sed_components`. Pure
>   post-processing; JIT-compatible for the first time.
>
> **Bridge fixes** that landed alongside the migration:
> `state_to_sed_quantities` switched `l_tir` to legacy semantics
> (`compute_l_tir(sed, wave)`) for parity (`fd07dee`). `Parameters`
> auto-infers `met_mode` from prior keys via a registry-driven
> discriminator table (`b1ff2c2`) — registered modes can now be
> selected without an explicit `met_mode=` arg.
>
> **Outstanding (not blocking) Phase II-3 closure**:
> - Final deletion of `_compute_sed_components` requires migrating
>   the 5 parity-check test consumers to a different pinning
>   strategy (e.g., snapshotted reference SEDs).
> - 5 declarable but unwired metallicity modes (`two_step`,
>   `psb_two_step`, `bins`, `bins_continuity`, `table`) — registry
>   has them, no consumer reads them in either path.
> - Cold-compile benchmark vs legacy.
>
> **Last updated**: 2026-05-06 — Phase II-3 closure complete.
>
> **Post-closure 2026-05-17** — Typed cross-component publish/require
> contract landed (ADR-0004 at authoring, now ADR-0009). `DerivedKey` in `tengri.protocols.component`;
> `validate_pipeline` in `tengri.forward.orchestrator` runs at
> `build_components` end and catches renames, units drift, missing
> publishers, and out-of-order publishers — with a "Did you mean: ..."
> Levenshtein hint. All 8 active components annotated with
> `publishes()`; `requires()` left `()` for opportunistic readers.
> Constants consolidation (Layer 0): `LSUN_ERG` alias removed from
> `utils/sed_quantities.py` public surface (one-release deprecation
> shim retained); internal AGN consumers continue to import
> `LSUN_ERG` from `_phys.py` (back-compat alias to `L_SUN`) pending a
> follow-up cleanup PR. A future chain reorder collapses to "edit one
> `publishes`/`requires` annotation"; see appendix brief #2 in
> `~/.claude/plans/witty-singing-garden.md` for the topological-sort
> follow-up plan.

## Why This Document Exists

Tengri grew organically without a design contract. The physics and math are excellent (geoVI,
PSD-correlated SFH, differentiable Ray Tracing). The engineering structure is fragmented. This
document records the root causes, what should have been done upfront, a complete inventory of the
current codebase, and a prioritized refactor plan with scoped subagent tasks.

---

## Complete Module Inventory (as of 2026-04-05)

This section documents every module in the codebase so the refactor plan is grounded in reality,
not aspiration.

### Top-level (`src/tengri/`)

| File | Lines | Purpose |
|------|-------|---------|
| `__init__.py` | 264 | Root package: JAX config, public API exports, `posteriors_to_dataframe()` |
| `distributions.py` | — | Distribution classes: `Uniform`, `Gaussian`, `LogUniform`, `LogNormal`, `StudentT`, `Fixed` |
| `plotting.py` | — | Plotting utilities: `plot_sed_fit`, `plot_sfh`, `plot_sfh_comparison`, `plot_spectrum_fit`, `plot_corner_comparison`, `safe_corner`, `diagnostics_table`, `setup_style`, `COLORS`, `SDSS_WAVE_EFF`, `SPECTRAL_FEATURES` |
| `simulate.py` | — | Simulation utilities |

### Core (`src/tengri/core/`) — 15 files

| File | Lines | Purpose |
|------|-------|---------|
| `model.py` | 2115 | **Still large.** High-level `Model` class: `__init__`, `from_config()`, `fit()`, forward predictions (`predict_photometry`, `predict_spectrum`, `predict_sed`), tier dispatch, parameter translation |
| `param_spec.py` | 1760 | **Still large.** `ParamSpec`/`Parameters` class: parameter registry, prior definitions, sampling, validation. Non-SFH params always present; SFH params resolved from registry |
| `settings.py` | 244 | Frozen dataclasses: `SFHConfig`, `DustConfig`, `NebularConfig`, `AGNConfig`, `MultiwavelengthConfig`, `ModelConfig`. Structural choices NOT in gradient tape |
| `convenience.py` | 562 | Extracted convenience methods: `mock()`, `mock_spectrum()`, `mock_batch()`, `fit_batch()`, `fit_catalog()`, `fit_population()`, `prior_predictive()` |
| `display.py` | 257 | Introspection helpers: `tree()`, `method_recommendation()` |
| `fused_kernels.py` | — | Tier 1/2 JIT kernel builders: `build_fused_photometry`, `build_fused_rest_sed`, `build_fused_spectrum`, `build_exact_sed`, `build_fused_tier2_photometry`, `build_fused_tier2_spectrum`, `observe_photometry_from_rest_sed`, `observe_spectrum_from_rest_sed`, `is_fused_compatible`, `is_tier2_compatible` |
| `mock.py` | — | `MockData` NamedTuple and mock generation (also exported from convenience) |
| `noise.py` | — | Noise model utilities: `compute_effective_noise`, `compute_std_inv`, `has_noise_model`, `uses_student_t`, `variable_noise_hamiltonian` |
| `param_translate.py` | — | Name translation layer: `_build_param_map`, `get_internal_params`, `LOG10_ZSUN`, evolving metallicity/alpha maps |
| `prediction.py` | — | Output containers: `Prediction`, `DerivedQuantities`, `EmissionLines`, `SEDQuantities`, `SFHQuantities` |
| `sed_pipeline.py` | — | SED computation engine: `compute_sed_components`, `interp_metallicity`, `interp_metallicity_evolving`, `get_agn_kwargs`, `get_dust_kwargs` |
| `sed_components.py` | 433 | Compositional rest-frame SED builders: `build_ssp_component()`, dust/agn/nebular component closures for Tier 2 |
| `precompute_templates.py` | 279 | Pre-integrate template models (DL07, SKIRTOR) through filters at init time |

### Inference (`src/tengri/inference/`) — 24 files

| File | Lines | Purpose |
|------|-------|---------|
| `fitter.py` | 1064 | `Fitter` class: `__init__`, `run()` dispatch, `compile()`, VI posterior draws, `fit_batch`; delegates loss builders to `loss_functions.py`, MAP/laplace/pathfinder to `map_dispatch.py`, MCMC/VI/NSS to submodules |
| `posterior.py` | — | `Posterior` class: samples, stats, plotting, convergence diagnostics |
| `hierarchical.py` | — | `PopulationFitter`/`HierarchicalFitter`, `PopulationPosterior`/`HierarchicalResult` |
| `vi.py` | 943 | Variational inference runners: `run_native_vi()` (JIT geoVI/MGVI with linear, geovi, nonlinear_update sample modes) |
| `geovi.py` | 230 | NIFTy.re geoVI backend (legacy, requires NIFTy) |
| `geovi_nuts.py` | — | geoVI-NUTS hybrid |
| `mcmc.py` | 345 | MCMC runners: `run_raytrace()`, `run_nuts()`, `run_elliptical_slice()` |
| `raytrace.py` | — | Ray Tracing sampler core (Behroozi 2025) |
| `nuts.py` | 196 | BlackJAX NUTS wrapper |
| `elliptical_slice.py` | 163 | BlackJAX Elliptical Slice Sampling |
| `map_optimizer.py` | 129 | MAP via optax (Adam) |
| `map_dispatch.py` | — | MAP dispatch helpers |
| `evidence.py` | 174 | Nested Slice Sampling (NSS) for Bayesian evidence |
| `laplace.py` | 151 | Laplace approximation (Gaussian from Hessian at MAP) |
| `pathfinder.py` | 134 | BlackJAX Pathfinder (L-BFGS path approximation) |
| `sbi.py` | 396 | Simulation-Based Inference infrastructure: training data generation, HDF5 I/O, `SBIPosterior` wrapper |
| `loss_functions.py` | — | `build_loglikelihood_fn`, `build_logprior_fn`, `build_loss_fn`, unbounded variants |
| `jit_engine.py` | — | JIT compilation engine for inference |
| `vi_config.py` | — | `VIConfig` dataclass for VI hyperparameters |
| `common.py` | — | Shared inference utilities: `InferenceResult`, `build_loss_fn`, `initialize_params` |
| `standardized.py` | — | Standardized parameter space utilities |
| `ns/` | — | Nested sampling subpackage (NSS implementation) |

### Models — SFH (`src/tengri/models/sfh/`) — 8 files

| File | Purpose |
|------|---------|
| `mean_sfh.py` | Analytic mean SFH functions: `dpl`, `tsnorm`, `snorm`, `norm`, `lnorm`, `delayed_tau`, `exponential_sfh`, `constant_sfh`, `triweight_burst` |
| `gp_sfh.py` | GP field generation: `generate_gp_fourier`, `generate_gp_batch`, `gp_from_xi`, `compute_sqrt_power_drw`, `make_log_age_grid` |
| `psd_models.py` | Power spectral density models: `psd_drw`, `drw_acf`, `drw_variance` (DRW), Matérn |
| `registry.py` | `SFH_REGISTRY`, `FIELD_MODEL_REGISTRY`, `resolve_sfh()`, `compute_field_gp()` |
| `chemical_evolution.py` | Gas-regulator Z(t) model |
| `nonparametric.py` | Non-parametric SFH support |

### Models — Dust (`src/tengri/models/dust/`) — 5 files

| File | Purpose |
|------|---------|
| `attenuation.py` | Dust attenuation laws: power_law, Calzetti, Kriek-Conroy, SMC, Cardelli, Salim, Li08, two-component Charlot & Fall. `precompute_dust_age_weights()` |
| `emission.py` | Dust emission models: modified blackbody, Casey2012, Dale2014, Draine & Li 2007/2014 |
| `priors.py` | Dust parameter priors |

### Models — AGN (`src/tengri/models/agn/`) — 13 files

| File | Purpose |
|------|---------|
| `agn_config.py` | `AGNConfig` dataclass |
| `disc.py` | Multicolor accretion disc models |
| `torus.py` | Dusty torus emission |
| `skirtor.py` | SKIRTOR clumpy torus model |
| `blr.py` | Broad-line region emission |
| `nlr.py` | Narrow-line region emission |
| `polar_dust.py` | Polar dust component |
| `unified.py` | Unified AGN model (NLR + BLR + torus + disc) |
| `qsogen.py` | QSOGEN AGN model |
| `_phys.py` | AGN physics constants and helpers |
| `_nthcomp.py` | nthcomp X-ray spectral model |

### Models — Nebular (`src/tengri/models/nebular/`) — 11 files

| File | Purpose |
|------|---------|
| `cloudy_cb19.py` | CLOUDY photoionization (Charlot & Bruzual 2019 grids) |
| `cloudy_grid.py` | HDF5 CLOUDY grid interpolation with bilinear (Z, logU) |
| `cue.py` | Cue analytic nebular emission model |
| `baked_in.py` | Baked-in emission (from SSP templates) |
| `agn_nebular.py` | AGN nebular emission |
| `dig.py` | Diffuse ionized gas (DIG) contribution |
| `ionizing_spectrum.py` | Ionizing photon production from SFH |
| `mappings_photo.py` | MAPPINGS shock+photoionization |
| `shock.py` | Shock emission models |

### Models — Observation (`src/tengri/models/observation/`) — 14 files

| File | Purpose |
|------|---------|
| `observation.py` | `Observation` container |
| `photometry_config.py` | `Photometry` dataclass |
| `spectroscopy_config.py` | `Spectroscopy`/`SpectroscopyConfig` dataclasses |
| `noise_config.py` | `NoiseConfig`/`NoiseModel` dataclasses |
| `filters.py` | Filter loading: `load_filter_set()` |
| `line_catalog.py` | `LineList`/`LineCatalog` emission line definitions |
| `photometry.py` | Photometry computation: `compute_flux_density`, `ab_mag_from_flux` |
| `spectroscopy.py` | Spectroscopy computation: `compute_spectrum`, `apply_lsf` |
| `calibration.py` | Calibration polynomial marginalization |
| `eline_priors.py` | Emission line priors: `cloudy_line_priors()`, `marginalize_emission_lines_cloudy()` |
| `eline_marginalization.py` | Emission line marginalization machinery |
| `eline_catalog.py` | Emission line catalog utilities |

### Models — SPS (`src/tengri/models/sps/`) — 5 files

| File | Purpose |
|------|---------|
| `dsps_wrapper.py` | DSPS integration: `SSPData`, `load_ssp_data`, `effective_metallicity`, `interpolate_met_alpha`, `salaris_feh_from_mh`, `salaris_mh_from_feh` |
| `precompute.py` | Precompute photometry/spectroscopy kernels: `precompute_photometry`, `precompute_photometry_ztable`, `precompute_spectroscopy` |
| `mass_remaining.py` | Stellar mass remaining computation (Lehmer+2010, Mineo+2012) |

### Models — Other

| File | Purpose |
|------|---------|
| `models/igm.py` | IGM Lyman absorption (Madau 1995) |
| `models/radio.py` | Radio synchrotron emission (FIR-radio correlation) |
| `models/xray.py` | X-ray emission (XRB scaling from SFR, AGN X-ray) |

### Diagnostics (`src/tengri/diagnostics/`) — 6 files

| File | Purpose |
|------|---------|
| `fisher.py` | Fisher information matrix analysis |
| `autocorrelation.py` | Autocorrelation time estimation |
| `saliency.py` | Saliency analysis |
| `green_functions.py` | Green's function diagnostics |

### Profiling (`src/tengri/profiling/`) — 5 files

| File | Purpose |
|------|---------|
| `timers.py` | Timing utilities |
| `memory.py` | Memory profiling |
| `pipeline.py` | Pipeline profiling |

### Utils (`src/tengri/utils/`) — 8 files

| File | Purpose |
|------|---------|
| `transforms.py` | Bounded↔unbounded parameter transforms: `to_bounded`, `to_unbounded` |
| `cosmology.py` | Cosmology: `luminosity_distance`, `age_at_z` |
| `grid.py` | Grid utilities: `make_log_age_grid`, `log_age_to_age_yr`, `grid_spacing`, `interpolate_to_linear_time` |
| `devices.py` | JAX hardware configuration |
| `optimizations.py` | Hartley transform, approximate photometry |
| `sed_quantities.py` | SED-derived quantity computations |

---

## Implementation Status

| Phase | Description | Status | Notes |
|-------|-------------|--------|-------|
| 1 | Structural Naming Contract | ✅ Complete | New names with `DeprecationWarning` on old |
| 2A | Split `fitter.py` (4305 → 1064 lines) | ✅ Complete | Dispatch table + algorithm submodules |
| 2B | Split `model.py` (2538 → 2115 lines) | ⚠️ Partial | Extracted `convenience.py` (562), `display.py` (257), `mock.py`, `prediction.py`. `model.py` still 2115 lines — needs further extraction of `__init__`/`from_config` logic |
| 3 | Settings/Parameters Split | ⚠️ Partial | `core/settings.py` exists with `SFHConfig`, `DustConfig`, `NebularConfig`, `AGNConfig`, `MultiwavelengthConfig`, `ModelConfig`. But `ParamSpec.__init__` (1760 lines) still accepts model selection flags — not fully migrated |
| 4 | Canonical Method Names | ✅ Complete | 9 canonical strings, aliases deprecated |
| 5 | Tier 2 SED Path | ✅ Complete | `build_fused_rest_sed`, tier-2 dispatch, `sed_components.py` (433 lines), benchmark tests |
| 6A | CLOUDY 2D Interpolation | ✅ Complete | Bilinear interpolation over all 4 (Z, logU) corners |
| 6B | Marginalization ln_L | ✅ Complete | Residual-shift approach with proper prior mean handling |
| 6C | Gradient Test Suite | ✅ Complete | `test_gradients.py` — 5 FD tests + direction + pipeline tests |
| 7 | Prune Documentation | ✅ Complete | `docs/models/` stub docs removed |

### Remaining Items (science / tests — not structural refactor)

- **BUG-29**: Fixed 2026-04-04 — `_mstar` now uses `compute_surviving_mass()` when SSP mass-remaining grid is available; fallback to formed mass otherwise. Both `mstar_formed` and `mstar_surviving` returned in output dict. Reference: Lehmer+2010 ApJS 189 1; Mineo+2012 MNRAS 419 2095.
- **Gradient test gaps**: All three gaps closed:
  - `cloudy_grid_line_priors()` — 8 tests added to `TestCloudyGridLinePriors` in `test_eline_priors.py` (NEW-07, fixed 2026-04-04)
  - `marginalize_emission_lines_cloudy` FD gradient — `test_gradient_matches_finite_difference` added to `TestMarginalizeEmissionLinesCloudy` (NEW-09, fixed 2026-04-04)
  - `cloudy_line_priors` bilinear interpolation gradient — `test_gradient_matches_finite_difference_log_z` and `test_gradient_matches_finite_difference_neb_logU` added to `TestCloudyLinePriors` (fixed 2026-04-05)

---

## Root Causes (in severity order)

### 1. No design contract before code

Class naming conventions were never agreed on. The result:

- `SpectroscopyConfig` is a data container (should be `Spectroscopy`)
- `NoiseConfig` is a statistical model (should be `NoiseModel`)
- `ParamSpec` is a parameter registry (should be `Parameters`)
- `HierarchicalFitter` is an orchestrator (should be `PopulationFitter`)
- `LineCatalog` sounds like a DB table (should be `LineList`)

Parameter naming grew into 3 layers with no explicit contract:

- Short user-facing: `log_peak_sfr`
- Full prefixed public: `sfh_tsnorm_log_peak_sfr`
- Internal: `log_peak_sfr` (same spelling, type-dependent meaning — silent bug factory)

Module boundaries were never defined. `fitter.py` became a 4305-line god file because nobody
decided what "inference" owns vs. what "model" owns.

### 2. Settings and parameters were never separated at the type level

Settings = *which sub-model is active* (dust law, nebular backend) — not in gradient tape.
Parameters = *scalars with priors that get fitted* — in gradient tape.

These are fundamentally different types. They were conflated in `ParamSpec` and `Model.__init__`,
making both enormous. `DustConfig`, `NebularConfig`, `AGNConfig` were designed as frozen
dataclasses much later and retrofitted. This is the single largest structural mistake.

### 3. Physics categories accumulated without defaults or selection guidance

- SFH models: 8+ (dpl, tsnorm, snorm, norm, lnorm, exp, dexp, const, field, field+burst)
- Dust attenuation: 6 laws
- Dust emission: 5+ models
- AGN: 4 models (multicolor_disc, unified_nlr_blr, kubota_done_full, qsogen)
- Inference: 14+ named methods for 5 distinct algorithms

No guidance on which to use. No recommended defaults. Each was added as "another option" rather
than replacing or superseding a prior option.

### 4. The "tier 2" SED path was never built

- **Tier 1**: Fused photometry (fixed-z + fixed filters) → ~140 µs, 20× speedup
- **Tier 2**: Compositional rest-frame SED (all physics, free-z, JIT'd) → ~300–500 µs ← **MISSING**
- **Tier 3**: Exact fallback (tabulated SFH, evolving Z, Python dispatch) → ~500–1000 µs

Designed in `docs/design_compositional_sed.md`. Never implemented. All non-tier-1 work falls
through to slow tier 3.

### 5. Gradient correctness was never systematically tested

All gradient tests check `isfinite()` only. They would pass with wrong signs or missing terms. The
CLOUDY marginalization bug (wrong ln_L, biases MAP/VI gradients) passed all tests.
`cloudy_grid_line_priors()` has zero coverage.

### 6. External dependency traps

NIFTy had 99.8% Python overhead (partial wrapping, type checking, dict dispatch). Required full
reimplementation in JAX primitives. The lesson: isolate external library APIs behind internal
abstractions from day one; do not let NIFTy types leak into inference logic.

### 7. Documentation outran implementation

`docs/models/` has full write-ups for: chemical evolution Z(t), shock emission (MAPPINGS), ADAF
disc, MAGPHYS dust, THEMIS dust, patchy IGM reionization, alpha-enhanced SSPs — all with zero or
near-zero code. Creates false scope expectations and makes the project feel unmanageable.

---

## What Should Have Been Done First (Pre-Implementation Checklist)

### Step 0A — Write 3 usage archetypes as runnable scripts

The scripts define every class name, method name, and parameter name before implementation. Run
them as the "north star."

```python
# Grad student: photometric fit, parametric SFH
model = SEDModel.from_config(
    ssp="fsps_default", sfh="dpl", dust="charlot_fall",
    filters=["sdss_u", "sdss_g", "sdss_r"], redshift=0.1,
)
result = model.fit(flux, noise)

# Expert: stochastic SFH, spectroscopy, MCMC validation
params = Parameters(sfh="field", dust="two_component", nebular="cloudy")
params.add("sfh_field_psd_sigma", LogUniform(0.1, 3.0))
result = SEDModel(ssp=grid, settings=settings, params=params, obs=obs).fit().refine("mcmc")

# Population: hierarchical PSD hyperparameters
pop = SEDModel.fit_population(
    obs_list, shared=["sfh_field_psd_sigma", "sfh_field_psd_tau_myr"]
)
pop.plot_population()
```

### Step 0B — Write the suffix convention table before any class definitions

| Suffix      | Meaning                                          | Examples                          |
|-------------|--------------------------------------------------|-----------------------------------|
| (none)      | Concrete class with behavior                     | `SEDModel`, `Fitter`, `Posterior` |
| `Config`    | Frozen settings, NOT in gradient tape            | `DustConfig`, `NebularConfig`     |
| `Backend`   | Swappable physics implementation                 | `CloudyBackend`, `CueBackend`     |
| `Model`     | Statistical model                                | `NoiseModel`, `CalibrationModel`  |
| `List`      | Registry/catalog                                 | `LineList`, `FilterList`          |
| `-er` suffix | Active orchestrator                             | `Fitter`, `PopulationFitter`      |

Forbidden: `Spec` (vague), `Config` on data containers, `Result` (use `Posterior`).

### Step 0C — Define the unit contract table

Every parameter docstring must state: user-facing unit / internal unit / conversion.

| Parameter           | User-facing | Internal   | Conversion  |
|---------------------|------------|------------|-------------|
| `sfh_field_psd_tau` | Myr        | yr         | × 10⁶       |
| `sfh_*_tau_peak`    | Gyr        | yr         | × 10⁹       |
| `met_logzsol`       | log(Z/Z☉)  | log(Z) abs | + LOG10_ZSUN |

### Step 0D — Draw the module boundary tree with ownership rules

**Target architecture** (aspirational — see [Current Architecture](#current-architecture-actual) for what exists today):

```
tengri/
├── core/
│   ├── model.py         < 300 lines: thin orchestrator, no physics
│   ├── parameters.py    parameter registry + prior definitions only
│   ├── settings.py      DustConfig, NebularConfig, AGNConfig
│   ├── param_map.py     name translation (one layer: public → internal)
│   ├── fused_kernels.py JIT kernel builders
│   ├── sed_pipeline.py  SED computation engine
│   └── noise.py         NoiseModel
├── inference/
│   ├── fitter.py        < 150 lines: dispatch table only
│   ├── map.py           MAP + optimizer
│   ├── vi.py            geoVI + MGVI (JIT path + NIFTy path)
│   ├── mcmc.py          NUTS + Ray Tracing + ESS
│   ├── evidence.py      Nested Slice Sampling
│   ├── population.py    PopulationFitter
│   └── posterior.py     Posterior + PopulationPosterior
└── models/              physics (unchanged structure, but with 1 default per category)
```

**Dependency rule**: `core/` never imports from `inference/`. `inference/` never imports from
`models/` directly (only through `core/`). Physics modules only import from `utils/`.

### Current Architecture (actual as of 2026-04-05)

```
tengri/
├── core/                          # Forward model orchestration
│   ├── model.py                   # 2115 lines — Model class (still too large)
│   ├── param_spec.py              # 1760 lines — ParamSpec/Parameters (still too large)
│   ├── settings.py                # 244 lines — Config dataclasses ✅
│   ├── convenience.py             # 562 lines — mock, fit_batch, fit_catalog, etc.
│   ├── display.py                 # 257 lines — tree(), method_recommendation()
│   ├── fused_kernels.py           # Tier 1/2 JIT kernel builders ✅
│   ├── sed_components.py          # 433 lines — Compositional SED builders ✅
│   ├── sed_pipeline.py            # SED computation engine
│   ├── prediction.py              # Output containers (Prediction, SEDQuantities, etc.)
│   ├── param_translate.py         # Name translation layer
│   ├── precompute_templates.py    # 279 lines — DL07/SKIRTOR template precomputation
│   ├── mock.py                    # MockData container
│   └── noise.py                   # Noise model utilities
├── inference/                     # Inference algorithms
│   ├── fitter.py                  # 1064 lines — Dispatch table + Fitter class
│   ├── posterior.py               # Posterior class
│   ├── hierarchical.py            # PopulationFitter, PopulationPosterior
│   ├── vi.py                      # 943 lines — Native JIT geoVI/MGVI
│   ├── geovi.py                   # 230 lines — NIFTy.re backend (legacy)
│   ├── geovi_nuts.py              # geoVI-NUTS hybrid
│   ├── mcmc.py                    # 345 lines — Ray Tracing, NUTS, ESS runners
│   ├── raytrace.py                # Ray Tracing sampler core
│   ├── nuts.py                    # 196 lines — BlackJAX NUTS
│   ├── elliptical_slice.py        # 163 lines — BlackJAX ESS
│   ├── map_optimizer.py           # 129 lines — MAP via optax
│   ├── map_dispatch.py            # MAP dispatch helpers
│   ├── evidence.py                # 174 lines — NSS for evidence
│   ├── laplace.py                 # 151 lines — Laplace approximation
│   ├── pathfinder.py              # 134 lines — BlackJAX Pathfinder
│   ├── sbi.py                     # 396 lines — SBI infrastructure
│   ├── loss_functions.py          # Log-likelihood, log-prior, loss builders
│   ├── jit_engine.py              # JIT compilation engine
│   ├── vi_config.py               # VIConfig dataclass
│   ├── common.py                  # Shared inference utilities
│   ├── standardized.py            # Standardized parameter space
│   └── ns/                        # Nested sampling subpackage
├── models/                        # Physics modules
│   ├── sfh/                       # Star formation histories
│   │   ├── mean_sfh.py            # Analytic SFH functions (dpl, tsnorm, etc.)
│   │   ├── gp_sfh.py              # GP field generation
│   │   ├── psd_models.py          # PSD models (DRW, Matérn)
│   │   ├── registry.py            # SFH_REGISTRY, FIELD_MODEL_REGISTRY
│   │   ├── chemical_evolution.py  # Gas-regulator Z(t)
│   │   └── nonparametric.py       # Non-parametric SFH
│   ├── dust/                      # Dust models
│   │   ├── attenuation.py         # 7 attenuation laws + two-component
│   │   ├── emission.py            # 5 emission models (MBB → DL14)
│   │   └── priors.py              # Dust parameter priors
│   ├── agn/                       # AGN models (13 files)
│   │   ├── disc.py                # Multicolor accretion disc
│   │   ├── torus.py               # Dusty torus
│   │   ├── skirtor.py             # SKIRTOR clumpy torus
│   │   ├── blr.py / nlr.py        # Line regions
│   │   ├── unified.py             # Unified model
│   │   ├── qsogen.py              # QSOGEN
│   │   ├── polar_dust.py          # Polar dust
│   │   ├── agn_config.py          # AGNConfig
│   │   ├── _phys.py / _nthcomp.py # Physics helpers
│   ├── nebular/                   # Nebular emission (11 files)
│   │   ├── cloudy_cb19.py         # CLOUDY CB19 grids
│   │   ├── cloudy_grid.py         # HDF5 grid interpolation
│   │   ├── cue.py                 # Cue analytic model
│   │   ├── baked_in.py            # Baked-in (SSP templates)
│   │   ├── agn_nebular.py         # AGN nebular
│   │   ├── dig.py                 # Diffuse ionized gas
│   │   ├── ionizing_spectrum.py   # Ionizing photon production
│   │   ├── mappings_photo.py      # MAPPINGS shock+photo
│   │   └── shock.py               # Shock emission
│   ├── observation/               # Observation models (14 files)
│   │   ├── observation.py         # Observation container
│   │   ├── photometry_config.py   # Photometry dataclass
│   │   ├── spectroscopy_config.py # Spectroscopy dataclass
│   │   ├── noise_config.py        # NoiseModel dataclass
│   │   ├── filters.py             # Filter loading
│   │   ├── line_catalog.py        # LineList/LineCatalog
│   │   ├── photometry.py          # Flux computation
│   │   ├── spectroscopy.py        # Spectrum computation
│   │   ├── calibration.py         # Calibration marginalization
│   │   ├── eline_priors.py        # Emission line priors
│   │   ├── eline_marginalization.py
│   │   └── eline_catalog.py
│   ├── sps/                       # Stellar population synthesis
│   │   ├── dsps_wrapper.py        # DSPS integration
│   │   ├── precompute.py          # Precompute kernels
│   │   └── mass_remaining.py      # Surviving mass computation
│   ├── igm.py                     # IGM Lyman absorption
│   ├── radio.py                   # Radio synchrotron
│   └── xray.py                    # X-ray emission (XRB + AGN)
├── diagnostics/                   # Convergence & analysis
│   ├── fisher.py                  # Fisher information matrix
│   ├── autocorrelation.py         # Autocorrelation time
│   ├── saliency.py                # Saliency analysis
│   └── green_functions.py         # Green's function diagnostics
├── profiling/                     # Performance profiling
│   ├── timers.py                  # Timing utilities
│   ├── memory.py                  # Memory profiling
│   └── pipeline.py                # Pipeline profiling
├── utils/                         # Shared utilities
│   ├── transforms.py              # Bounded↔unbounded transforms
│   ├── cosmology.py               # Luminosity distance, age_at_z
│   ├── grid.py                    # Grid utilities
│   ├── devices.py                 # JAX hardware config
│   ├── optimizations.py           # Hartley transform, approx photometry
│   └── sed_quantities.py          # SED-derived quantities
├── distributions.py               # Distribution classes
├── plotting.py                    # Plotting utilities
└── simulate.py                    # Simulation utilities
```

**Key deviations from target:**
- `model.py` is 2115 lines (target: < 300) — `__init__` and `from_config` still contain physics-adjacent logic
- `param_spec.py` is 1760 lines (target: split into parameters.py + settings.py) — settings exist but ParamSpec still accepts model selection flags
- `fitter.py` is 1064 lines (target: < 150) — dispatch table is clean but still contains helper methods
- Inference has grown beyond original plan: added `laplace.py`, `pathfinder.py`, `sbi.py`, `geovi_nuts.py`, `ns/` subpackage
- `sed_components.py` (433 lines) and `precompute_templates.py` (279 lines) are new additions not in original plan
- `display.py` (257 lines) extracted for introspection — good split

### Step 0E — Define the parameter namespace before any Parameters code

```
{sfh_type}_{param}      sfh_dpl_alpha, sfh_field_psd_sigma
met_{param}             met_logzsol
dust_{param}            dust_tau_bc, dust_tau_diff, dust_slope
neb_{param}             neb_logU, neb_logZ_gas, neb_fesc
agn_{submodel}_{param}  agn_disc_log_mbh, agn_torus_frac
eline_{param}           eline_broad
noise_{param}           noise_f_cal, noise_dof
```

No short names in the public API except `logzsol` → `met_logzsol` (documented explicitly). No
internal names that shadow public names.

---

## Prioritized Refactor Plan

### Phase 1 — Structural Naming Contract (P0, Non-Breaking) ✅ COMPLETE

Add new names alongside old with `DeprecationWarning`. Remove old names in v1.0.

| Current                  | Rename to             | Reason                                          |
|--------------------------|-----------------------|-------------------------------------------------|
| `ParamSpec`              | `Parameters`          | Vague abbreviation                              |
| `SpectroscopyConfig`     | `Spectroscopy`        | Data container, not config                      |
| `NoiseConfig`            | `NoiseModel`          | It's a statistical model                        |
| `HierarchicalFitter`     | `PopulationFitter`    | "Hierarchical" is an implementation detail      |
| `HierarchicalResult`     | `PopulationPosterior` | "Hierarchical" is an implementation detail      |
| `LineCatalog`            | `LineList`            | Sounds like a database table                    |
| `Posterior.summary()`    | `Posterior.stats()`   | Returns dict (inconsistent with other `.summary()` → str) |

**Files**: `core/param_spec.py`, `inference/fitter.py`, `inference/posterior.py`,
`inference/hierarchical.py`, `models/observation/line_catalog.py`, `__init__.py`

---

### Phase 2 — Split God Files (P0) ✅ COMPLETE

#### 2A: Split `fitter.py` (4305 → 1064 lines) → inference subpackage

**What was done:**
- `inference/fitter.py` (1064 lines) — Dispatch table + `Fitter` class with helper methods
- `inference/vi.py` (943 lines) — `run_native_vi()` (JIT geoVI/MGVI with linear, geovi, nonlinear_update modes)
- `inference/mcmc.py` (345 lines) — `run_raytrace()`, `run_nuts()`, `run_elliptical_slice()`
- `inference/evidence.py` (174 lines) — `run_nss()` for Bayesian evidence
- `inference/map_optimizer.py` (129 lines) — MAP via optax
- `inference/raytrace.py` — Ray Tracing sampler core
- `inference/nuts.py` (196 lines) — BlackJAX NUTS wrapper
- `inference/elliptical_slice.py` (163 lines) — BlackJAX ESS

**Beyond the original plan (added during organic growth):**
- `inference/geovi.py` (230 lines) — NIFTy.re geoVI backend (legacy, requires NIFTy import)
- `inference/geovi_nuts.py` — geoVI-NUTS hybrid
- `inference/laplace.py` (151 lines) — Laplace approximation (Gaussian from Hessian at MAP)
- `inference/pathfinder.py` (134 lines) — BlackJAX Pathfinder (L-BFGS path approximation)
- `inference/sbi.py` (396 lines) — SBI infrastructure: training data generation, HDF5 I/O, `SBIPosterior`
- `inference/loss_functions.py` — Log-likelihood, log-prior, loss builders
- `inference/jit_engine.py` — JIT compilation engine
- `inference/vi_config.py` — `VIConfig` dataclass
- `inference/common.py` — Shared inference utilities (`InferenceResult`, `build_loss_fn`, etc.)
- `inference/standardized.py` — Standardized parameter space utilities
- `inference/map_dispatch.py` — MAP dispatch helpers
- `inference/ns/` — Nested sampling subpackage

**What remains (fitter.py still ~1064 lines; original target < 150 was not met):**
- **Already delegated:** `build_loss_fn`, `build_logprior_fn`, `build_loglikelihood_fn`, `build_loglikelihood_unbounded_fn` live in `loss_functions.py` (Fitter keeps thin `_build_*` / `_get_or_build_*` caches). `_run_map` / `_run_laplace` / `_run_pathfinder` → `map_dispatch.py`. `_run_raytrace` / `_run_nuts` / `_run_elliptical_slice` → `mcmc.py`. `_run_native_vi` → `vi.py`. `_run_nifty_vi` → `vi.run_nifty_vi`. `_build_jit_engine` → `jit_engine.py`. `_run_nss` → still dispatched from `run()` but implementation is in `evidence.py`.
- **Still in `fitter.py`:** large `__init__` (eline/calibration wiring), `_data_args`, engine cache, `compile()`, `run()` routing and kwargs handling, `fit_batch()`, and VI posterior sample helpers (`_draw_jit_samples`, `_draw_blackjax_samples`, etc.).

**Next step (optional):** Move posterior-draw helpers and/or `compile()` into `vi.py` or a small `fitter_compile.py`; move init/unbounded helpers to `common.py` or `init_params.py`. Realistic near-term target: `fitter.py` < 600 lines, not < 150.

#### 2B: Split `core/model.py` (2538 → 2115 lines)

**What was done:**
- `core/convenience.py` (562 lines) — `mock()`, `mock_spectrum()`, `mock_batch()`, `fit_batch()`, `fit_catalog()`, `fit_population()`, `prior_predictive()`
- `core/display.py` (257 lines) — `tree()`, `method_recommendation()`
- `core/mock.py` — `MockData` NamedTuple
- `core/prediction.py` — `Prediction`, `DerivedQuantities`, `EmissionLines`, `SEDQuantities`, `SFHQuantities`
- `core/sed_components.py` (433 lines) — Compositional SED component builders for Tier 2
- `core/precompute_templates.py` (279 lines) — DL07/SKIRTOR template precomputation

**What remains (model.py still 2115 lines):**
- `__init__` contains extensive setup logic: tier selection, kernel building, dust age weight precomputation, CSP matrix setup, parameter map construction
- `from_config()` is a large factory method with branching for all physics options
- `predict_photometry()`, `predict_spectrum()`, `predict_sed()` still contain tier dispatch + fallback logic
- `_build_forward_model()` and related internal methods are still inline

**Next step:** Extract `__init__` setup into a `_ModelBuilder` class or separate `_init_*.py` modules. Target: `model.py` < 500 lines.

---

### Phase 3 — Settings/Parameters Split (P0, Breaking) ⚠️ PARTIAL

**What was done:**
- `core/settings.py` (244 lines) with frozen dataclasses:
  - `SFHConfig(mean_type, n_grid, evolving_metallicity, alpha_fe_evolving, chem_evol, met_interp, lgmet_scatter)`
  - `DustConfig(model, law_bc, law_diff, emission, approx)`
  - `NebularConfig(backend, grid_path, ...)`
  - `AGNConfig(...)`
  - `MultiwavelengthConfig(...)`
  - `ModelConfig(sfh, dust, nebular, agn, multiwavelength)`

**What remains (ParamSpec still 1760 lines):**
- `ParamSpec.__init__` still accepts `mean_sfh_type`, `nebular`, `dust_emission`, `radio`, `igm` flags
- These flags should be migrated to `ModelConfig` / `SFHConfig` / `NebularConfig` / `MultiwavelengthConfig`
- `_NON_SFH_PARAMS` dict (line 68+) defines default priors for all non-SFH parameters — this is a parameter concern, not a settings concern, but the two are still entangled
- The `add()` method handles both parameter registration and model selection side effects

**Migration path:**
1. Make `ModelConfig` the primary interface for model selection (done via `Model.from_config()`)
2. Deprecate model selection flags in `ParamSpec` / `Parameters.__init__` with warnings once call sites can migrate without noise — **deferred**: flags like `mean_sfh_type` are still used everywhere; blanket `DeprecationWarning` would drown signal. Prefer `Model.from_config(...)` for new code.
3. In v1.0, remove model selection flags from `ParamSpec` entirely — it becomes a pure parameter registry

---

### Phase 4 — Canonical Method Names (DONE — do not change)

The canonical inference method strings are settled and documented. They are **not** candidates
for further collapse into a `method+backend` flag API.

**Current canonical names** (as of 2026-04-05):

| Canonical string  | Algorithm                          | Deprecated aliases removed in v1.0      |
|-------------------|------------------------------------|-----------------------------------------|
| `"vi"`            | geoVI (NIFTy, **default**)         | `geovi`, `native_geovi`, `fast_geovi`   |
| `"vi_linear"`     | MGVI / EVI (linear response)       | `mgvi`, `native_mgvi`, `fast_mgvi`      |
| `"vi_nifty"`      | NIFTy geoVI with full logging      | `nifty_geovi`                           |
| `"vi_nifty_linear"` | NIFTy MGVI with full logging     | `nifty_mgvi`                            |
| `"vi_native"`     | Native JIT geoVI (500× faster)     | `native_geovi` (promoted)               |
| `"vi_native_linear"` | Native JIT MGVI                 | `native_mgvi` (promoted)                |
| `"mcmc_raytrace"` | Ray Tracing HMC (Behroozi 2025)    | `raytrace`                              |
| `"mcmc_nuts"`     | No-U-Turn Sampler (blackjax)       | `nuts`                                  |
| `"mcmc_ess"`      | Elliptical Slice Sampling          | `elliptical_slice`                      |
| `"evidence"`      | Nested Slice Sampling (log Z)      | `nss`                                   |
| `"map"`           | MAP point estimate                 | —                                       |
| `"laplace"`       | Laplace approximation              | —                                       |
| `"pathfinder"`    | BlackJAX Pathfinder                | —                                       |

**Why this design is better than `method="vi", backend="nifty"`:**

1. **Strings are unambiguous.** `fitter.run("vi_nifty")` is self-documenting in a notebook cell or
   log line. `fitter.run("vi", backend="nifty")` requires the reader to hold two arguments in mind
   simultaneously and understand the cross-product of valid `(method, backend)` combinations.

2. **Not all combinations are valid.** `method="mcmc", sampler="nuts"` and
   `method="mcmc", sampler="raytrace"` differ in more than the sampler — they have different keyword
   arguments (`n_warmup` vs `n_steps`, `step_size`, etc.). Flattening them into a shared namespace
   creates an argument collision problem. String dispatch keeps the call signatures independent.

3. **Paper reproducibility.** MCMC results in the paper are cited as "mcmc\_raytrace with
   `n_steps=1000`". That string appears in notebooks, CLAUDE.md, and the methods section. Changing
   it to `method="mcmc", sampler="raytrace"` would break that traceability without benefit.

4. **The proliferation was already solved.** The 14+ old method strings were aliases for 5 distinct
   algorithms. The aliases are now deprecated with `DeprecationWarning` and removed in v1.0. What
   remains is 13 canonical strings for 13 distinct dispatch paths — not proliferation.

**Action:** Remove `_DEPRECATED_METHOD_ALIASES` dict from `fitter.py` in v1.0. Update any
remaining alias callsites in `tests/` and `notebooks/` at that time. No API change needed now.

**Note on geoVI vs native:** `vi` (NIFTy geoVI) remains the **default** method. The native JIT
path (`vi_native`) is 500× faster but has not been validated against NIFTy across all model
configurations. Once validation is complete, the default will switch to `vi_native` and NIFTy
will become the optional fallback.

---

### Phase 5 — Build Tier 2 SED Path (P1) ✅ COMPLETE

Implement `build_fused_rest_sed(settings)` factory in `core/fused_kernels.py`:

- Produces rest-frame SED at SSP resolution, end-to-end JIT'd (~300–500 µs)
- Supports ALL physics components (unlike tier 1 which requires fixed-z)
- Called at `SEDModel.__init__` with graceful tier-3 fallback
- Dispatch logic: tier-1 check (fixed-z + photometry) → tier-2 check (all components JIT-able?)
  → tier-3 exact fallback

Component subfunctions to extract from `core/sed_pipeline.py`:

```python
_compute_stellar_sed(params, ssp, sfh_weights)
_apply_dust_attenuation(sed, params, config)
_add_dust_emission(sed, params, config)
_add_agn(sed, params, config)
_add_nebular(sed, params, config, nebular_backend)
```

**Files**: `core/fused_kernels.py`, `core/sed_pipeline.py`, `core/model.py`

---

### Phase 6 — Science Correctness Fixes (P0, Run in Parallel with Phases 1–2) ✅ COMPLETE

**6A: CLOUDY 2D interpolation** (`models/observation/eline_priors.py:162–173`)

- Bug: `cloudy_line_priors()` did 1D interpolation over Z only; missed logU dimension
- Fix: Replaced with 2D bilinear interpolation over all 4 (Z, logU) grid corners
- Test: Interpolated values match reference grid values at grid points

**6B: Marginalization ln_L normalization** (`models/observation/eline_priors.py:191–271`)

- Bug: `marginalize_emission_lines_cloudy` missing `+0.5 × (μ² / σ²)` normalization term for
  non-zero-mean prior — biases MAP/VI gradients; MCMC unaffected (uses likelihood ratios)
- Fix: Residual-shift approach with proper prior mean handling; finite-difference gradient test added

**6C: Gradient correctness test infrastructure** (`tests/unit/test_gradients.py`)

- Every transform inside a JIT boundary needs a finite-difference test
- Suite: 5 FD correctness tests + 3 direction tests + 2 pipeline composition tests
- All tests run without SSP data (mock inputs), suite completes in < 30 s

---

### Phase 7 — Prune Documentation (P2) ✅ COMPLETE

Moved all model documentation with zero code from `docs/models/` → `ROADMAP.md`:

- `docs/models/chemical_evolution.md`
- `docs/models/shock_emission.md`
- `docs/models/adaf_disc.md`
- `docs/models/magphys_dust.md`
- `docs/models/themis_dust.md`
- `docs/models/patchy_igm.md`
- `docs/models/pah_features.md` (partial: keep API design, move implementation section)

The `docs/models/` directory no longer exists.

---

## Subagent Scopes (Phases 1 & 4–7 complete; 2B & 3 partial)

Scopes A and F–G are complete. B and C delivered major splits but did not reach original line-count targets; D–E (science fixes) are complete. All scopes are independent except G (depended on C completing first).

### Subagent A — Naming Refactor (Phase 1) ✅ COMPLETE

**Scope**: Rename `ParamSpec→Parameters`, `SpectroscopyConfig→Spectroscopy`,
`NoiseConfig→NoiseModel`, `HierarchicalFitter→PopulationFitter`,
`HierarchicalResult→PopulationPosterior`, `LineCatalog→LineList`,
`Posterior.summary()→Posterior.stats()`.

**Strategy**: Add new names with `DeprecationWarning` on old; update `__init__.py` exports;
update all tests, notebooks, and CLAUDE.md.

**Files**: `core/param_spec.py`, `inference/fitter.py`, `inference/posterior.py`,
`inference/hierarchical.py`, `models/observation/line_catalog.py`, `__init__.py`, `tests/`,
`notebooks/`

**Constraint**: Non-breaking — old names still work, just warn.

---

### Subagent B — Split fitter.py (Phase 2A) ✅ COMPLETE

**Scope**: Extract `_run_fast_vi` + `_run_native_vi` + `_run_nifty_vi` →
`inference/vi.py`; extract `_run_raytrace` + `_run_nuts` + `_run_elliptical_slice` →
`inference/mcmc.py`; extract `_run_nss` → `inference/evidence.py`; extract MAP logic →
`inference/map_dispatch.py` / `map_optimizer.py`. Original goal was a < 150-line `fitter.py`;
delivered major extractions but `fitter.py` remains ~1064 lines (see Phase 2A “What remains”).

**Files**: `inference/fitter.py`, `inference/vi.py`, `inference/mcmc.py`,
`inference/evidence.py`, `inference/map_dispatch.py`, `inference/loss_functions.py`, `inference/__init__.py`

**Constraint**: All tests must pass. Zero behavior changes.

---

### Subagent C — Split model.py (Phase 2B) ✅ COMPLETE

**Scope**: Extract convenience methods (`prior_predictive`, `fit_batch`, `fit_population`,
`fit_catalog`) → `core/convenience.py`. Leave `model.py` as thin orchestrator < 400 lines.

**Files**: `core/model.py`, `core/convenience.py` (new), `core/__init__.py`

**Constraint**: Public API unchanged. All tests pass.

---

### Subagent D — Science Fix: CLOUDY 2D Interpolation (Phase 6A) ✅ COMPLETE

**Scope**: Fix `cloudy_line_priors()` in `models/observation/eline_priors.py:169–180` to
perform true bilinear interpolation over the (logZ_gas, logU) grid.

**Root cause**: The function blended `ratios_logU3` (Z-varying, logU=-3) with
`ratios_solar_u` (solar Z only, logU=-2). At `u_frac=1` (logU=-2) the result was pure solar
regardless of `log_z` — the metallicity dimension was completely dropped at one grid edge.
The fix added a fourth grid point `_CLOUDY_SUBSOLAR_LOGU2` and wired proper bilinear
interpolation:
```python
ratios_logu3 = lerp(subsolar_u3, solar_u3, z_frac)   # Z interpolation at logU=-3
ratios_logu2 = lerp(subsolar_u2, solar_u2, z_frac)   # Z interpolation at logU=-2
result = lerp(ratios_logu3, ratios_logu2, u_frac)    # logU interpolation
```

**Files**: `models/observation/eline_priors.py`, `tests/unit/test_eline_priors.py`

**Note**: This is NOT in `models/nebular/cloudy_grid.py` — that file handles the HDF5 grid
interpolation via `cloudy_grid_line_priors()`, which is a separate function. The bug is in
the simpler analytic prior `cloudy_line_priors()` in `eline_priors.py`.

**Constraint**: New interpolation path must pass finite-difference gradient check. Regression
test from `docs/known_bugs.md` (NEW-01) must be green after fix:
```python
def test_cloudy_priors_metallicity_effect_at_high_logu():
    means_solar, _ = cloudy_line_priors(log_z=0.0, neb_logU=-2.0)
    means_subsolar, _ = cloudy_line_priors(log_z=-0.7, neb_logU=-2.0)
    assert means_subsolar[8] < 0.5 * means_solar[8]  # [NII]6583 weaker at sub-solar Z
```

---

### Subagent E — Science Fix: Marginalization ln_L (Phase 6B) ✅ COMPLETE

**Scope**: Fix `marginalize_emission_lines_cloudy` in `models/observation/eline_priors.py` to
include missing `+0.5 × μ² / σ²` normalization term in ln_L for non-zero-mean prior.

**Files**: `models/observation/eline_priors.py`, `tests/unit/test_eline_priors.py`

**Constraint**: MCMC is unaffected (likelihood ratios cancel normalization). MAP/VI gradients
must be correct per finite-difference check after fix.

---

### Subagent F — Gradient Test Infrastructure (Phase 6C) ✅ COMPLETE

**Scope**: Create `tests/unit/test_gradients.py` with finite-difference gradient checks for:
(1) CSP mass weights, (2) dust attenuation transforms, (3) nebular emission marginalization,
(4) IGM transmission, (5) AGN disc spectrum. Use `jax.test_util.check_grads` or manual FD
with `eps=1e-5`.

**Files**: `tests/unit/test_gradients.py` (new)

**Constraint**: Tests must run without SSP data (use mock inputs). Full suite must complete in
< 30 s.

---

### Subagent G — Tier 2 SED Path (Phase 5, depends on Subagent C) ✅ COMPLETE

**Scope**: Implement `build_fused_rest_sed(settings)` factory in `core/fused_kernels.py`.
Extract 5 component subfunctions from `core/sed_pipeline.py`. Wire tier-2 dispatch in
`core/model.py`. Add benchmark test asserting free-z forward pass < 600 µs on CPU.

**Files**: `core/fused_kernels.py`, `core/sed_pipeline.py`, `core/model.py`

**Constraint**: Tier-1 (precomputed photometry) behavior unchanged. Tier-3 fallback still works
for tabulated SFH and evolving metallicity.

---

## Items Not Covered Above

### Physics Model Inventory: What's Implemented vs. Documented

This table catalogs every physics model in the codebase, its implementation status, and whether
it has a recommended default. This addresses Root Cause #3 (physics categories accumulated without
defaults or selection guidance).

#### Star Formation Histories

| Model | File | Status | Recommended? | Notes |
|-------|------|--------|-------------|-------|
| Double Power Law (DPL) | `mean_sfh.py` | ✅ Complete | ✅ Yes (default) | `sfh="dpl"`, params: alpha, beta, tau_gyr, log_peak_sfr |
| Truncated Skew-Normal (tsnorm) | `mean_sfh.py` | ✅ Complete | ✅ Yes | `sfh="tsnorm"`, params: log_peak_sfr, peak_lbt_gyr, width_gyr, skew, trunc |
| Delayed-τ | `mean_sfh.py` | ✅ Complete | — | `sfh="delayed_tau"`, params: tau_gyr, log_peak_sfr |
| Log-Normal | `mean_sfh.py` | ✅ Complete | — | `sfh="lnorm"`, params: peak_lbt_gyr, width_gyr, log_peak_sfr |
| Skew-Normal | `mean_sfh.py` | ✅ Complete | — | `sfh="snorm"` |
| Normal | `mean_sfh.py` | ✅ Complete | — | `sfh="norm"` |
| Exponential | `mean_sfh.py` | ✅ Complete | — | `sfh="exponential_sfh"` |
| Constant | `mean_sfh.py` | ✅ Complete | — | `sfh="constant_sfh"` |
| GP Field (DRW) | `gp_sfh.py` + `psd_models.py` | ✅ Complete | ✅ Yes (stochastic) | `mean_type=["dpl", "field"]`, params: psd_sigma, psd_tau_myr |
| GP Field (Matérn) | `psd_models.py` | ✅ Complete | — | Alternative PSD shape |
| Chemical Evolution Z(t) | `chemical_evolution.py` | ✅ Complete | — | Gas-regulator model, derives Z(t) from SFH |
| Evolving Metallicity | `param_translate.py` | ✅ Complete | — | Two-endpoint Z ramp via `SFHConfig.evolving_metallicity` |
| Non-parametric | `nonparametric.py` | ✅ Complete | — | Tabulated SFH support |

#### Dust Attenuation

| Model | File | Status | Recommended? | Notes |
|-------|------|--------|-------------|-------|
| Power Law | `attenuation.py` | ✅ Complete | ✅ Yes (default BC) | `dust_slope` param |
| Calzetti | `attenuation.py` | ✅ Complete | — | Starburst attenuation curve |
| Kriek-Conroy | `attenuation.py` | ✅ Complete | — | δ parameter for slope variation |
| SMC | `attenuation.py` | ✅ Complete | — | Small Magellanic Cloud curve |
| Cardelli | `attenuation.py` | ✅ Complete | — | R_V parameterized MW curve |
| Salim | `attenuation.py` | ✅ Complete | — | |
| Li08 | `attenuation.py` | ✅ Complete | — | |
| Two-Component (Charlot & Fall) | `attenuation.py` | ✅ Complete | ✅ Yes (default geometry) | τ_BC + τ_diff, separate young/old |
| Single-Component (screen) | settings | ✅ Complete | — | Uniform dust screen |

#### Dust Emission

| Model | File | Status | Recommended? | Notes |
|-------|------|--------|-------------|-------|
| Modified Blackbody | `emission.py` | ✅ Complete | — | T_dust, β_IR params |
| Casey 2012 | `emission.py` | ✅ Complete | — | |
| Dale 2014 | `emission.py` | ✅ Complete | — | Template-based |
| Draine & Li 2007 | `emission.py` | ✅ Complete | ✅ Yes (detailed) | U_min, q_PAH, gamma params. Precomputed via `precompute_templates.py` |
| Draine & Li 2014 | `emission.py` | ✅ Complete | — | Updated DL model |

#### AGN

| Model | File | Status | Recommended? | Notes |
|-------|------|--------|-------------|-------|
| Multicolor Disc | `disc.py` | ✅ Complete | ✅ Yes | M_BH, L/L_Edd, spin, inclination |
| SKIRTOR Clumpy Torus | `skirtor.py` | ✅ Complete | ✅ Yes | tau_skirtor, covering_frac, inclination |
| Unified (NLR+BLR+Torus+Disc) | `unified.py` | ✅ Complete | — | Full unified model |
| QSOGEN | `qsogen.py` | ✅ Complete | — | Empirical AGN SED |
| BLR | `blr.py` | ✅ Complete | — | Broad emission lines |
| NLR | `nlr.py` | ✅ Complete | — | Narrow emission lines |
| Polar Dust | `polar_dust.py` | ✅ Complete | — | Polar dust component |
| nthcomp X-ray | `_nthcomp.py` | ✅ Complete | — | X-ray spectral model |

#### Nebular Emission

| Model | File | Status | Recommended? | Notes |
|-------|------|--------|-------------|-------|
| CLOUDY CB19 | `cloudy_cb19.py` | ✅ Complete | ✅ Yes (detailed) | Photoionization grid |
| CLOUDY HDF5 Grid | `cloudy_grid.py` | ✅ Complete | ✅ Yes | Bilinear (Z, logU) interpolation |
| Cue Analytic | `cue.py` | ✅ Complete | ✅ Yes (fast) | Analytic approximation |
| Baked-in (SSP) | `baked_in.py` | ✅ Complete | — | From SSP templates |
| AGN Nebular | `agn_nebular.py` | ✅ Complete | — | AGN ionization |
| DIG | `dig.py` | ✅ Complete | — | Diffuse ionized gas |
| MAPPINGS | `mappings_photo.py` | ✅ Complete | — | Shock+photoionization |
| Shock | `shock.py` | ✅ Complete | — | Shock emission |

#### Multi-wavelength

| Model | File | Status | Recommended? | Notes |
|-------|------|--------|-------------|-------|
| IGM (Madau 1995) | `igm.py` | ✅ Complete | ✅ Yes (z > 0.5) | Lyman absorption |
| Radio Synchrotron | `radio.py` | ✅ Complete | ✅ Yes (radio data) | q_IR, alpha_sf, radio_loudness |
| X-ray (XRB) | `xray.py` | ✅ Complete | ✅ Yes (X-ray data) | L_X from SFR (Mineo+2012) |

#### Inference Methods

| Method | File | Status | Recommended? | Notes |
|--------|------|--------|-------------|-------|
| geoVI (NIFTy — **default** `method="vi"`) | `geovi.py` via `fitter.run` | ✅ Complete | ✅ Yes | Validated across model configs |
| MGVI / linear response (`method="vi_linear"`) | `vi.py` + NIFTy path | ✅ Complete | ✅ Yes | Default linear VI; native JIT variant below |
| geoVI / MGVI (native JIT) | `vi.py` | ✅ Complete | ✅ Yes (when validated for your setup) | `vi_native`, `vi_native_linear` — much faster; compare to NIFTy if unsure |
| geoVI / MGVI (NIFTy + full logging) | `geovi.py` | ✅ Complete | — | `vi_nifty`, `vi_nifty_linear` |
| geoVI-NUTS hybrid | `geovi_nuts.py` | ✅ Complete | — | geoVI warm start + NUTS |
| Ray Tracing HMC | `mcmc.py` + `raytrace.py` | ✅ Complete | ✅ Yes (stochastic, D ~ 137) | `method="mcmc_raytrace"` |
| NUTS (BlackJAX) | `mcmc.py` + `nuts.py` | ✅ Complete | ✅ Yes (D < 20, smooth) | `method="mcmc_nuts"` |
| Elliptical Slice | `mcmc.py` + `elliptical_slice.py` | ✅ Complete | — | `method="mcmc_ess"`, Gaussian priors only |
| MAP (optax) | `map_optimizer.py` | ✅ Complete | ✅ Yes (quick check) | `method="map"` |
| Laplace | `laplace.py` | ✅ Complete | ✅ Yes (D < 15, instant) | `method="laplace"` |
| Pathfinder | `pathfinder.py` | ✅ Complete | — | `method="pathfinder"`, warm start for MCMC |
| NSS (evidence) | `evidence.py` + `ns/` | ✅ Complete | — | `method="evidence"`, parametric only |
| SBI infrastructure | `sbi.py` | ✅ Complete | — | Training data gen, not training |

### BUG-29: `_mstar` uses formed mass, not surviving mass — FIXED 2026-04-04

`compute_surviving_mass(weights, ssp_mass_remaining)` now called when the SSP file provides
a mass-remaining grid (`ssp_data.ssp_mass_remaining`). Fallback to formed mass (`jnp.sum(weights)`)
when grid unavailable. Both `mstar_formed` and `mstar_surviving` returned in output dict.
`_mstar` alias points to `_mstar_surviving` for XRB calibration.

**Reference:** Lehmer+2010 ApJS 189 1 Table 2; Mineo+2012 MNRAS 419 2095 §3.1.

### Phase 7 clarification: what to do with each stub doc

"Move to ROADMAP.md" is underspecified. Concrete rule:

- **Delete** the implementation section entirely (code examples, parameter tables).
- **Keep** a 1–3 sentence physical motivation blurb in `ROADMAP.md` under a `## Planned
  Physics Modules` heading.
- **Keep** the API design section only if the interface contract was agreed on and is stable
  (e.g. if a `NebularConfig` already exists that would wrap it).

Files affected: `docs/models/chemical_evolution.md`, `shock_emission.md`, `adaf_disc.md`,
`magphys_dust.md`, `themis_dust.md`, `patchy_igm.md`, `pah_features.md` (partial).

### Phase 6C gradient test gaps (all closed 2026-04-05)

- `cloudy_grid_line_priors()` — 8 tests in `TestCloudyGridLinePriors` (`test_eline_priors.py`) ✅
- `marginalize_emission_lines_cloudy` FD gradient — `test_gradient_matches_finite_difference` in `TestMarginalizeEmissionLinesCloudy` ✅
- `cloudy_line_priors` bilinear interpolation gradient — two FD tests in `TestCloudyLinePriors` (`test_gradient_matches_finite_difference_log_z`, `test_gradient_matches_finite_difference_neb_logU`) ✅

---

## Rule for New Features Going Forward

Before adding any new model or inference method:

1. **Write the docstring first.** If you cannot describe it in 3 lines, the abstraction is wrong.
2. **Add the parameter to the namespace table** in `docs/parameter_reference.md`. If the name
   clashes or doesn't fit the prefix scheme, rename before writing code.
3. **Write a finite-difference gradient test.** If the transform is not differentiable, it cannot
   go inside a JIT boundary.
4. **If the code does not exist, do not document it in `docs/models/`.** Put it in `ROADMAP.md`.
